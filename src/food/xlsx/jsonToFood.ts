/*
import { Food, User, Prisma } from '@prisma/client';
import { XlsxService } from './xlsx.service';


export class jsonToFood {
  constructor (private readonly xlsxService: XlsxService) {}

  jsonToFood(jsonArray: JSON[]): Prisma.FoodCreateInput[] {
    const result = [];
    jsonArray.forEach(json => {
      const data: Prisma.FoodCreateInput = {
        name: json.name,

      }
      result.push({

      })
    })
    return result;
  }
}
*/
